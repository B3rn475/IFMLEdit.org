/*jslint node: true, nomen: true */
"use strict";

exports.createEvents = function (options) {
    return {
<% for (var i = 0; i < events.length; i += 1) { -%>
        <%= i !== 0 ? ',' : ''%>'<%= events[i].id %>': require('./<%= events[i].id %>').createEvent(options)
<% } -%>
    };
};
