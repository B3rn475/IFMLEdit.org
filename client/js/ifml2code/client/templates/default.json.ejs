[
<% for(var i=0; i < Math.pow(4, Math.max(fields.length, 1)); ++i) { -%>
    <%= i !== 0 ? ',': '' %>{ "_id": <%= i %>, "id": "<%= i %>"<% for(var f=0; f < fields.length; ++f) { %>, "<%= fields[f] %>": "<%= Math.floor(i / Math.pow(4, fields.length - f - 1)) % 4 %>"<% } %>}
<% } -%>
]
